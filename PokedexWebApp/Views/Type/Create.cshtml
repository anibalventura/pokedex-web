@model Business.ViewModels.SaveTypeViewModel
@{
    bool editMode = Model.Id == 0 ? true : false;
    ViewData["Title"] = editMode ? "Create Type" : "Edit Type";
}

<div class="row">
    <div class="col-5 offset-3">
        <div class="card shadow-sm">
            <div class="card-header bg-dark text-light">
                <h4>@(editMode ? "Create a Type" : $"Editing the Type: {Model.Name}")</h4>
            </div>
            <div class="card-body">
                <form method="post" asp-controller="Type" asp-action="@(editMode ? "Create" : "Edit")">

                    <input type="hidden" asp-for="Id" />

                    <div class="form-group mb-3">
                        <div>
                            <label asp-for="Name" class="form-label">Name: </label>
                            <input type="text" asp-for="Name" placeholder="Enter a name..." class="form-control shadow-sm">
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="container-fluid text-center">
                        <button type="submit" class="btn btn-success shadow-sm">Save</button>
                        <a asp-controller="Type" asp-action="Index" class="btn btn-secondary shadow-sm">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
