@using Business.ViewModels;
@model List<TypeViewModel>
@{
    ViewData["Title"] = "Types";
}

<div class="row justify-content-center">
    <div class="col-11">
        <div class="card shadow-sm">
            <div class="card-header bg-primary bg-opacity-75">
                <h3 class="text-white">
                    Types Maintenance
                    <a class="btn btn-success shadow-sm" asp-controller="Type" asp-action="Create">Create</a>
                </h3>
            </div>
            @if (Model.Count == 0 || Model == null)
            {
                <div class="container-fluid text-center m-5">
                    <h4>No Types created...</h4>
                    <h5>Start creating one!</h5>
                </div>
            }
            else
            {
                <div class="card-body">
                    <div class="row">
                        @foreach (TypeViewModel item in Model)
                        {
                            <div class="col-2 mb-3">
                                <div class="card shadow-sm">
                                    <div class="card-body">
                                        <h4>@item.Name</h4>
                                        <div class="d-flex justify-content-between align-items-center">
                                            <div class="btn-group">
                                                <a asp-controller="Type" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Edit</a>
                                                <a asp-controller="Type" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            }
        </div>
    </div>
</div>
